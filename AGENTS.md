# Agent 协作规则

## 项目概况

这是一个使用 **Jekyll TeXt Theme** 构建的个人技术博客，部署目标为 GitHub Pages。

- `_posts/`：正式博客文章，文件名使用 `YYYY-MM-DD-slug.md`。
- `draft/`：草稿和待整理内容，不作为正式文章提交。
- `_layouts/`、`_includes/`：页面布局和可复用模板。
- `_sass/`、`assets/`：样式、脚本和静态资源。
- `_data/`：导航、语言等站点数据。
- `_config.yml`：Jekyll 主配置，修改后需要重新启动本地服务。
- `blog_sync/`：向外部博客平台同步文章的 Python 工具及配置示例。
- `docs/`：主题演示和文档站点资源，不应与个人博客文章混淆。
- `test/`、`scripts/`、`tools/`：测试、构建辅助脚本和工具。

项目级技能位于 `.agents/skills/`，只对本仓库生效：

- `.agents/skills/refine-draft/SKILL.md`：审查素材、规划并生成结构化技术博客草稿。
- `.agents/skills/publish-post/SKILL.md`：将草稿补充 Jekyll front matter、整理资源并发布到 `_posts/`。

涉及草稿整理时优先使用 `refine-draft`；用户明确要求发布时使用 `publish-post`。两者均不得直接执行 Git 提交、推送或外部平台发布。

### 技能调用路由

- 用户提供 `draft/` 下的一篇或多篇材料，并要求“总结并输出 blog”、整理草稿或生成博文时，优先调用 `refine-draft`。该技能负责素材审查、文章划分、提纲确认和 Markdown 草稿生成。
- `refine-draft` 完成后，可提示用户是否需要发布。提示只作为下一步建议，不自动执行发布。
- 用户明确要求“发布文章”、将草稿放入 `_posts/` 或同步为正式 Jekyll 文章时，调用 `publish-post`。该技能负责 front matter、文件名、章节编号、图片资源和发布前检查。
- `refine-draft` 生成的文件默认位于 `draft/refined/`，`publish-post` 以用户确认的草稿为输入并写入 `_posts/`。两个技能之间通过明确的文件路径和文件名交接。

## 工作边界

1. 先阅读目标文件及其相关模板、配置，再进行修改。
2. 保持改动范围最小，不无关重排文件，不修改生成目录 `_site/` 和缓存 `.jekyll-cache/`。
3. 不提交密钥、Cookie、访问令牌或真实平台配置。外部同步配置应从 `blog_sync/config.example.json` 复制并在本地填写。
4. 修改文章时保留已有 front matter、永久链接、图片路径和 `<!--more-->` 摘要分隔符，除非用户明确要求调整。
5. 修改主题代码时兼容现有 Jekyll/Ruby 依赖；优先复用已有变量、布局和 Sass 结构。
6. 完成后检查 `git diff`，确认没有意外文件、凭据或大体积生成物。

## 博客整理与输出规范

当用户提供对话记录、零散笔记或技术材料时，将其整理为结构严谨的 Markdown 技术博客。

### 输出格式

- 输出必须直接从 `# 博客主标题` 开始。不要寒暄、开场白、结语或过程说明。
- 使用 H2/H3 标题组织层次。推荐结构为：概念定义 → 底层原理 → 代码示例 → 实践与注意事项。
- 使用 **加粗** 标出核心概念或重要结论。
- 概念对比使用 Markdown 表格。
- 代码块必须标注语言，例如 ` ```cpp `；专业术语、API、变量名使用行内代码。
- 保留必要的底层逻辑、场景分析、推导过程和限制条件，不为追求简短而丢失关键事实。

### 文风

- 使用直接、简洁的书面技术语言，优先短句和直接陈述句。
- 禁止口语化表达，例如“算出来”“写函数写循环”“说白了”“其实就是”“比如说”。
- 禁止比喻、拟人和情绪化修辞；避免在比喻语境使用“缩影”“黑洞”“极致”“演化”“篡改”“剥离”“下达指令”等词。
- 删除无实义的强调词，例如“严格的”“彻底”“绝对”“有权”“正是”“深度”（修辞用途）。
- 重组材料的逻辑顺序，消除重复、铺垫和无依据的结论。技术事实不确定时明确标注，不擅自补造。

## 文章编辑检查

提交文章前确认：

- front matter 至少包含正确的 `title`；需要时保留 `tags`、`article_header` 等主题字段。
- 日期、文件名、标题和链接一致，图片使用仓库内已有路径或可访问的绝对 URL。
- 摘要分隔符 `<!--more-->` 放在合适的位置，正文没有遗留对话元信息。
- 标题层级连续，代码块语言正确，表格和链接语法可解析。
- 未引入未经用户提供或来源不明的事实。

## 验证命令

优先使用以下命令验证改动：

- `bundle exec jekyll build`：构建站点。
- `bundle exec jekyll serve -H 0.0.0.0`：本地预览。
- `npm run eslint`：检查 `_includes/**/*.js`。
- `npm run stylelint`：检查 `_sass/**/*.scss`。

根据改动范围运行必要检查，并在交付时说明执行的命令及结果。若本地缺少 Ruby、Bundler 或依赖，说明阻塞原因，不要擅自升级依赖或删除锁文件。

## Git 约定

提交信息遵循 Conventional Commits（如 `feat: add post`、`fix: correct link`、`docs: update article`）。提交前查看 `git diff` 和 `git status`；除非用户明确要求，不执行提交、推送、发布或删除历史操作。
